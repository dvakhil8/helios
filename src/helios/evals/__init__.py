"""Helios eval harness — runs the agent against suboptimal Databricks jobs
and scores its optimizations against ground-truth annotations.

Layout mirrors the tools/ convention: one file per concern. Scorers live under
scorers/ and are also one file per tier.
"""
